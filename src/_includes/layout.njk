<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }} | Blake Porter</title>
  <meta name="description" content="{{ description or excerpt }}">

  <link rel="canonical"
        href="{{ canonicalURL or ('https://blake-porter.com' ~ page.url) }}" />

  <!-- Open Graph + Twitter Card omitted for brevity -->

  <!-- CORE STYLES -->
  <link rel="stylesheet" href="/resources/css/style.css">

  <!-- PER-PAGE STYLES (via front-matter `css` array) -->
  {% if css %}
    {% for sheet in css %}
      <link rel="stylesheet" href="/resources/css/{{ sheet }}.css">
    {% endfor %}
  {% endif %}

  {% block head %}{% endblock %}
</head>

<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>
  {% include "partials/header.html" %}

  {% block content %}
    {% if bodyClass == "homepage" or bodyClass == "inspiration-page" %}
      {{ content | safe }}
    {% else %}
      <main class="site-main container">
        {{ content | safe }}
      </main>
    {% endif %}
  {% endblock %}

  {% include "partials/footer.html" %}
  <script src="/resources/js/main.js" defer></script>
  <script src="/resources/js/guides.js" defer></script>
</body>
</html>
