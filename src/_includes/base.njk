<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  {# Link to your compiled CSS #}
  <link rel="stylesheet" href="/assets/css/style.css" />
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>
  {# Include your header partial if you have one (or drop this line) #}
  {% if headerVariant %}{% include "header.njk" %}{% endif %}

  {# Insert page content here #}
  {{ content | safe }}

  {# Include your footer partial if you have one (or drop this line) #}
  {% include "footer.njk" %}

  {# Link to your JavaScript #}
  <script src="/assets/js/main.js" defer></script>
</body>
</html>
